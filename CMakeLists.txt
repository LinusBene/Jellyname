cmake_minimum_required(VERSION 3.12)
set(TARGET jellyname)
project(${TARGET})
add_executable(${TARGET} main.c)

if(MSVC)
  target_compile_options(${TARGET} PRIVATE /W4 /WX)
else()
  target_compile_options(${TARGET} PRIVATE -Wall -Wextra -Wpedantic)
endif()
